# requirements.txt — RegulaAI (Python 3.11 совместимый, гибкие версии)
# =============================================================================
# Core
# =============================================================================
streamlit>=1.28.0
requests>=2.31.0
openai>=1.0.0
# =============================================================================
# Data & Analytics
# =============================================================================
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
plotly>=5.15.0
openpyxl>=3.1.0
matplotlib>=3.7.0
# =============================================================================
# Vector Database & Embeddings
# =============================================================================
chromadb>=0.4.0
langchain-community>=0.0.10
langchain-text-splitters>=0.0.1
sentence-transformers>=2.2.0
# =============================================================================
# Hybrid Search & Reranking (итерация 2)
# =============================================================================
rank-bm25>=0.2.2
pymorphy3>=1.2.0
# =============================================================================
# Document Processing
# =============================================================================
pypdf>=3.15.0
docx2txt>=0.8
python-docx>=1.0.0
easyocr>=1.7.0
opencv-python-headless>=4.8.0
PyMuPDF>=1.23.0
mammoth>=1.6.0
# =============================================================================
# Utilities
# =============================================================================
packaging>=21.0
protobuf>=3.20.0
rich>=10.14.0
tenacity>=8.0.0
zstandard>=0.21.0
# =============================================================================
# Аутентификация
# =============================================================================
bcrypt>=4.0.0
PyJWT>=2.7.0
extra-streamlit-components>=0.1.60
# =============================================================================
# Протокольщик — транскрибация
# =============================================================================
faster-whisper>=0.10.0
# CUDA-библиотеки для CTranslate2 (backend faster-whisper) — GPU-инференс
# требует libcublas.so.12 и libcudnn, которых нет в python:3.11-slim.
# Эти pip-пакеты содержат нужные .so-файлы, путь к ним прописан в Dockerfile
# через LD_LIBRARY_PATH.
nvidia-cublas-cu12
nvidia-cudnn-cu12
# =============================================================================
# Optional (закомментировано намеренно)
# =============================================================================
# speechrecognition>=3.10.0
# openai-whisper>=20231117  # Требует компиляции — не нужен, используем faster-whisper